# 서울픽 블로그 운영

## 현재 상태

- `data/blog-drafts.json`에 노션의 기획 30개를 발행순서, 주제, 분류, 영문 제목·키워드, 독자 질문과 각 행의 상세 기획까지 저장했습니다.
- 이 파일은 공개 화면에서 가져오지 않습니다. 기획 메모와 체크리스트는 기사 본문이 아닙니다.
- 공개 글은 `data/blog.ts`의 `posts` 배열에만 등록합니다. 현재 공개 글은 0개입니다.
- 노션의 발행순서는 편집 우선순위이며 발행일이 아닙니다. 자동 발행 일정은 없습니다.

## 글 작성 후 공개하는 절차

1. 노션 기획 행과 `data/blog-drafts.json`의 동일한 `notionUrl`을 참고하여 완성 원고를 작성합니다. 시간·요금·교통·예약·언어 지원은 해당 공식 출처에서 새로 확인하고 확인 날짜를 기록합니다.
2. 사용 권한이 확인된 대표 사진과 본문 사진을 프로젝트의 `public/photos`에 넣고 사진 출처 페이지에 작가·라이선스·원본 링크를 추가합니다.
3. `data/blog.ts`의 `posts` 배열에 `PublishedPost` 객체를 추가합니다. 고유 `slug`, 한국어 제목·요약·태그·6개 분류 중 하나, 보관용 영문 제목·키워드, **실제** `publishedAt`·`updatedAt`, 대표 이미지, 본문 `sections`, 공식 `sources`·`verifiedAt`, FAQ, 관련 장소·코스·글의 실제 ID를 입력합니다. `action`은 실제 열리는 내부 주소가 있을 때만 넣습니다.
4. 원고와 사실관계, 사진 권한, 링크를 검수하고 노션 행의 상태를 `발행완료`로 변경하며 실제 `/blog/[slug]` 주소를 `발행URL`에 기록합니다. 해당 행을 `data/blog-drafts.json`에서 제거합니다.
5. `pnpm check:blog`, `pnpm typecheck`, `pnpm build`를 실행합니다. 목록·분류·검색·상세·관련 링크·사이트맵을 PC와 모바일에서 확인한 뒤 GitHub에 푸시하여 연결된 Vercel 프로젝트에 게시합니다.

발행일은 기획순서나 노션 수정일에서 만들지 않습니다. 영어 본문을 실제로 작성·검수하기 전에는 영문 주소나 `hreflang`을 공개하지 않습니다. 노션 인증 정보는 저장소와 브라우저 코드에 넣지 않습니다.
